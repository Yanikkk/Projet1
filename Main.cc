#include <iostream>
#include "Env.hpp"


int main() {
	
	Env riviere(2000, 3000, 20000); 
	// créer un objet Env avec les dimensions x, y, z passées en paramètres.
	
	
	/*delete[] tableau_;*/ // pas oublier de supprimer le tableau -> ici ?
	return 0;
}

